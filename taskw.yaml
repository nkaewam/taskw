version: "1.0"

paths:
    output_dir: .
    scan_dirs: [.]

generation:
    dependencies:
        enabled: true
        output_file: dependencies_gen.go
    # This project is a CLI tool, so we don't need to generate routes
    routes:
        enabled: false
        output_file: routes_gen.go
