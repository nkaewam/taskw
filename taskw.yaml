version: "1.0"

paths:
    output_dir: ./internal/cli
    scan_dirs: [./internal]

generation:
    dependencies:
        enabled: true
        output_file: dependencies_gen.go
    # This project is a CLI tool, no need to generate routes
    routes:
        enabled: false
        output_file: routes_gen.go
